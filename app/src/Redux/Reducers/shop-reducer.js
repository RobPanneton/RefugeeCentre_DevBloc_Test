const initialState = {
  shopInv: null,
  cart: {},
  loadStatus: "",
  shopIndex: 0,
};

export const shopReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
